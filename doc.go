/*
Package cron is a cron manager partially based on github.com/robfig/cron (same parser semantics). It ads more fine-grained control on running jobs, such
as cancellations, as well as persistent storage of entries.
*/
package cron
